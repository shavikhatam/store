<%= simple_form_for [:admin, @item] do |f| %>
    <%= f.input :name %>
    <%= f.input :description %>
    <%= f.input :price %>
    <%= f.input :category_id, collection: Category.all %>
    <%= f.input :image %>
    <%= f.submit %>

<% end %>